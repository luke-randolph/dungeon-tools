# @dungeon-tools/api

Placeholder for future Vercel functions. The mobile app currently runs entirely
on-device (bundled SRD spell catalog + local SQLite). When the AI assistant
feature ships, this package will host the routes that proxy Claude calls
through Vercel AI Gateway so API keys never live on user devices.

Not implemented in v1.
